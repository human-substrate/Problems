#!/usr/bin/env bun
import { ROOT, invariant, write } from "./lib/io.ts";
const index=await Bun.file(`${ROOT}/index.json`).json(),keys=Object.keys(index.series).sort();
const S:Record<string,any>={};
for(const k of keys){S[k]=await Bun.file(`${ROOT}/series/${k}.json`).json();invariant(S[k]._meta.key===k&&S[k]._meta.coverage===index.series[k].coverage,`catalog mismatch ${k}`);}
const deferred:Record<string,string>=await Bun.file(`${ROOT}/deferred.json`).json();
const GROUPS:[string,string[]][]=[
  ["Reported to police — FBI CDE",["motorVehicleTheftRate","burglaryRate","robberyRate","aggravatedAssaultRate"]],
  ["Experienced crime — NCVS",["ncvsViolentRate","ncvsPropertyRate","ncvsReportedShare"]],
  ["Justice-system response",["jailRate","correctionalSupervisionRate","executions","deathRowPopulation","federalPrisonPopulation","federalOffendersSentenced"]],
  ["What people believe — Gallup and GSS",["gallupMoreCrime","gallupAfraidToWalk","gssFearWalking","gssFavorDeathPenalty","gssCourtsNotHarsh","gssGunInHome"]],
  ["Online reports and losses",["fraudReports","identityTheftReports","ic3Complaints","ic3Losses"]],
];
invariant(GROUPS.flatMap(([,ks])=>ks).filter(k=>S[k]).length===keys.length,"all series placed in documentation");
const last=(k:string)=>{const ys=Object.keys(S[k].data),y=ys.at(-1)!;return {y,v:S[k].data[y],first:ys[0],n:ys.length};};
const row=(k:string)=>{const m=S[k]._meta,l=last(k);return `| [${m.name}](series/${k}.json) | ${l.v} (${l.y}) | ${m.unit} | ${m.coverage} · ${l.n} points | ${m.class} |`;};
const tables=GROUPS.map(([title,ks])=>`## ${title}\n\n| Series | Latest | Unit | Coverage | Class |\n|---|---:|---|---|---|\n${ks.filter(k=>S[k]).map(row).join("\n")}`).join("\n\n");
const defTable=`| Key | Disposition |\n|---|---|\n${Object.keys(deferred).sort().map(k=>`| ${k} | Deferred: HTTP 429 OVER_RATE_LIMIT; see deferred.json |`).join("\n")}`;
const rules="Values come from sealed cached primary sources. No network, interpolation, or smoothing. Gaps are retained. Computed series state their formulas; fiscal years use the fiscal-year number. Do not equate beliefs, police reporting, survey victimization, correctional response, and voluntary online complaints. Each series carries its own unit, breaks, method, bounds, and source URLs. A named 12-point exception applies to jailRate; all other shipped series have at least 15 observations.";
await write("README.md",`# US Crime and Justice\n\n${keys.length} annual U.S. series shipped; ${Object.keys(deferred).length} requested keys deferred. Generated by docs.ts from index.json and series JSON; never edit this file directly.\n\n${rules}\n\n## Reproduce\n\n\`\`\`sh\nbun --env-file=$HOME/.claude/.env update.ts\nbun test lib/checks.test.ts\nbun docs.ts\nbun update.ts --only ncvs,gss\n\`\`\`\n\nBun and the existing .cache snapshot are required; there are zero npm dependencies. update.ts verifies SHA-256 hashes against sources.json before parsing. It refuses changed or missing inputs and continues independent groups, exiting nonzero for unexpected errors. --only accepts group names or individual keys and merges the index. Groups: audit, fbi, ncvs, corrections, capital, federal, gallup, gss, ftc, ic3. update.log is appended on each build, including deferred-key evidence. FBI CDE rows use FBI_API_KEY when present, else DEMO_KEY (rate-limited; a 429 defers with the live body).\n\n## Files\n\n- index.json: catalog with coverage, point counts, latest values, and metadata.\n- series/<key>.json: {_meta, data}, ascending four-digit year keys.\n- sources.json: raw-cache URLs, read date, and immutable SHA-256 fingerprints. Preserve .cache for offline replay.\n- deferred.json: evidenced deferrals (empty when every key ships).\n- lib/checks.test.ts: 19 tests pinned to cached publisher values or independently computed GSS shares.\n- source.md: detailed provenance and methodology.\n- work/recovery.md and work/mismatches.md: recovery evidence, rejected stitches, and sample differences.\n\n${tables}\n\n## Deferred\n\n${defTable}\n\nSnapshot read: ${index.sourceRead}. Build: ${index.generated}.\n`);
await write("SUMMARY.md",`# US Crime and Justice — Summary\n\n${keys.length} shipped series and ${Object.keys(deferred).length} deferred keys. Latest values below are derived directly from the shipped JSON.\n\n${tables}\n\n## Interpretation\n\n${rules}\n\nNCVS reported share is computed from rounded rates and can differ from BJS's directly published percentage. IC3 values preserve the source's stated precision. Coverage endpoints do not imply every intervening year is available. Source and recovery limitations are recorded per series in source.md.\n\n## Deferred\n\n${defTable}\n\nBuild: ${index.generated}.\n`);
const sources=keys.map(k=>{const m=S[k]._meta;return `## ${m.name} (\`${k}\`)\n\n- Source: ${m.source}\n- URL: <${m.sourceUrl}>\n- Historical editions: ${(m.historicalSourceUrls??[]).map((u:string)=>`<${u}>`).join(" · ")||"Same source"}\n- Coverage: ${m.coverage}; ${last(k).n} points\n- Unit: ${m.unit}; class: ${m.class}\n- Annual rule: ${m.annualRule}\n- Method: ${m.method}\n- Breaks: ${m.breaks}\n- Note: ${m.note}\n- Source read: ${m.sourceRead}; build timestamp: ${m.fetched}`;}).join("\n\n");
await write("source.md",`# Sources — US Crime and Justice\n\nGenerated from index.json and series/*.json by docs.ts. sources.json contains cached-file URLs, read dates, and SHA-256 hashes. Builds parse only the local snapshot; fetched is the build timestamp, not a claim of a new network download.\n\n${sources}\n\n## Deferred evidence\n\n${Object.entries(deferred).map(([k,e])=>`### ${k}\n\n\`\`\`text\n${e}\n\`\`\``).join("\n\n")}\n`);
console.log(`README.md + SUMMARY.md + source.md regenerated for ${keys.length} series`);
